
function titleCase(str) {
  var arr = [];
  arr = str.split(" ");
  capArr = [];
  
  for(var i = 0; i < arr.length; i++){
    
    capArr.push(arr[i].charAt(0).toUpperCase() + arr[i].slice(1).toLowerCase());
  }
  
  capArr = capArr.join(" ");
  
  return capArr;
}

titleCase("sHoRt AnD sToUt");
